# This project has two separate .env files -- copy each into place:
#
#   backend/.env.example    -> backend/.env      (FastAPI settings)
#   frontend/.env.example   -> frontend/.env.local (Next.js settings)
#
# See README.md for setup instructions. Nothing here needs to be edited to
# run the demo locally -- all defaults are already safe.
